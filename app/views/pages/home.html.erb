<div class="home-callout text-center container-fluid">
    <h1>Welcome to DevMatch!</h1>
    <h3>A membership site where Entrepreneurs can meet Developers</h3>
</div>

<div class="container home-signup-links">
    <div class="row">
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Basic Membership</h2>
                <br>
                <h4 class="text-center">Sign up for free and get basic access to the community</h4>
                <br><br>
                <!--(plan: xxxxx) creates a query parameter to be sent with that link to be used by the next page-->
                <%= link_to "Sign up with Basic plan", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Pro Membership</h2>
                <br>
                <h4 class="text-center">Sign up for the Pro account for $10/month and get access to the community and contact information!</h4>
                <br>
                <!--(plan: xxxxx) creates a query parameter to be sent with that link to be used by the next page-->
                <%= link_to "Sign up with Pro plan", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg  btn-block' %>
            </div>
        </div>
    </div>
    <!--Using an 'if' to show the profile link ONLY if logged in-->
    <%= link_to 'Create your profile', new_user_profile_path(user_id: current_user.id), class: 'btn btn-info btn-lg' if user_signed_in?%>
</div>